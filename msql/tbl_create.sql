CREATE TABLE IF NOT EXISTS Data(
	time DATETIME,
	location varchar(50),
	IEEE varchar(20),
	ESSID varchar(20),
	Frequency varchar(20),
	Access_Point varchar(20),
	Bit_Rate varchar(20),
	Tx_Power varchar(20)
);